// if we export default then we can use it importing by any name because that file is only that is exported default/ default export file can be one only;

function add(a,b){
    console.log(a+b);
}

export default add;